# Backend - Shiv Dhaba Food Delivery System

Spring Boot 3.x backend for food delivery system.

## 🚀 Quick Start

1. **Prerequisites**
   - Java 17
   - MySQL 8.x
   - Redis 7.x
   - Maven 3.8+

2. **Database Setup**
   ```sql
   CREATE DATABASE food_delivery_db;
   ```

3. **Configuration**
   - Update `src/main/resources/application.properties` with your credentials
   - Add Razorpay keys
   - Add Firebase service account JSON path

4. **Run**
   ```bash
   mvn clean install
   mvn spring-boot:run
   ```

## 📡 API Base URL
`http://localhost:8080/api/v1`

See main README.md in root for complete API documentation.


